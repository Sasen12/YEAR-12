# Optional extra OCR engines beyond built-in RapidOCR + Tesseract fallback.
# Install these only when you need additional model diversity.
easyocr==1.7.1
paddleocr==2.6.1.3
torch>=2.2.0
transformers==4.44.2
