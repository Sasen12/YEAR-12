# Optional OCR engines (install only if you need them; they pull heavy deps).
# Note: Wheels may not exist for Python 3.14; prefer Python 3.10/3.11 or Docker.
easyocr==1.7.1
paddleocr==2.6.1.3
torch>=2.2.0
transformers==4.44.2
